import React from "react";

const Button = ({ name }) => {
  return (
    <div className="w-auto">
      <button className="px-5 m-1 py-1.5 bg-gray-200 rounded-lg transition-all ease-in-out hover:bg-gray-300">
        {name}
      </button>
    </div>
  );
};

export default Button;

<button className="">Add</button>;
